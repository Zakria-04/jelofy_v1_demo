// import { useParams } from "next/navigation";
import React from "react";
import RestaurantAuthSlug from "./RestaurantAuthSlug";

const page = () => {
//   const { restaurantSlug } = useParams<{ restaurantSlug: string }>();
  return <RestaurantAuthSlug />;
};

export default page;
